# Clarion / MSBuild build artifacts
*.exe
*.map
*.pdb
*.obj
*.res
*.dcd
*.sln.cache
*.suo
obj/
bin/

# Clarion runtime DLL copied in by the build (ships with Clarion, don't redistribute)
ClaRUN.dll

# CodeGraph databases (generated snapshots; can be large)
*.codegraph.db
*.codegraph.db-shm
*.codegraph.db-wal
!/Samples/*.codegraph.db

# Editor / OS cruft
*~
Thumbs.db
.vs/

# NOTE: sqlite3.dll and sqlite3.lib are intentionally committed (dependencies).
